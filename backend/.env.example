# Puerto del servidor backend
PORT=5000

# URL base de la API de OpenWA
OPENWA_API_URL=http://localhost:8002
OPENWA_TOKEN=cambia_esto_por_tu_api_key
OPENWA_SESSION_ID=cambia_esto_por_tu_session_id

# JWT Secret (genera uno aleatorio)
JWT_SECRET=genera_un_secreto_seguro_aqui

# Base de datos SQLite (ruta del archivo)
DATABASE_URL="file:./dev.db"
